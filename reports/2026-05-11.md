# Daily Report — 2026-05-11

## Summary

No version bump today — TS SDK remains at v0.2.138 and Python SDK at v0.1.80; monitor detected only registry dedup artifacts. The update agent ran successfully (66s, 17 turns) and made three notable doc improvements: added `suppressOriginalPrompt` and `effort` field documentation to the TS hook reference, added a V2 session API deprecation warning, and updated the Python `sandbox_config.py` template to document the KI #27 `can_use_tool` + AsyncIterable prompt requirement. Both research agents completed without finding new issues above the scan high-water marks (TS #236, PY #776). The typecheck step reported exit code 1 — the same recurring stale-comma false-positive in the import resolution check — and was overridden to `success` in outcomes. Verify passed 2/2 on the first attempt with no mending.

## Pipeline Status

| Step | Result | Duration | Notes |
|------|--------|----------|-------|
| Monitor | ✅ success | 8s | Changes detected (registry dedup artifacts) |
| Auth | ✅ success | — | method: unknown |
| Update | ✅ success | 66s | Doc updates; no version bump |
| Research (TS) | ✅ success | 223s | No new issues above #236 |
| Research (PY) | ✅ success | 265s | No new issues above #776 |
| Typecheck | ⚠️ exit 1 → overridden success | 15s | Recurring stale-comma false-positive in import check |
| Verify | ✅ success | 0s | 2/2 checks passed; 0 mend runs |
| Report | 🔄 running | — | |

## Monitor

- **Changes detected**: yes
- Change types detected:
  - `npm_version` (TypeScript): registry returned multiple version entries (0.2.138, 0.2.94, 0.2.112 → resolved to 0.2.138) — deduplication artifact, no actual change
  - `pypi_version` (Python): registry returned multiple entries (0.1.80, 0.1.45 → resolved to 0.1.80) — deduplication artifact, no actual change
- No issue state changes or new bug issues detected

## Update Agent

No version bump applied (TS 0.2.138 → 0.2.138, PY 0.1.80 → 0.1.80). The agent made the following documentation improvements:

**SKILL-typescript.md** (3 changes):
1. `UserPromptSubmit` hook output: Added `suppressOriginalPrompt: true` field (when `decision` is `"block"`, omits the original prompt from the block message) and updated section comment
2. Hook input common fields: Expanded description to include `effort?` field — the active effort level for the current turn (e.g. `{ level: "high" }`); present for tool-use context hooks on effort-supporting models; also exposed as `CLAUDE_EFFORT` env var
3. V2 Session API section: Added deprecation warning — `unstable_v2_createSession`, `unstable_v2_resumeSession`, and `unstable_v2_prompt` are officially `@deprecated`; users should use `query()` instead

**SKILL-python.md**:
- Updated "Last verified" date to 2026-05-11

**templates/python/sandbox_config.py**:
- Updated `production_sandbox()` docstring and code to document KI #27: `can_use_tool` requires AsyncIterable prompt since v0.1.78+; using a string prompt raises `ValueError`; switched the example to use `prompt_gen()` async generator

Verification passed on attempt 1 with 0 mend runs.

## Research

### TypeScript Research (issues scanned up to #236)

No new issues added above the high-water mark. Research confirmed all recent issues are already tracked or not actionable.

### Python Research (issues scanned up to #776)

No new issues added above the high-water mark. Research confirmed all recent issues are already tracked or not actionable.

## Errors

### Typecheck — exit code 1 (recurring false-positive, overridden to success)

**Last output:**
```
  OK:   query_with_tools.py — valid syntax
  OK:   sandbox_config.py — valid syntax
  OK:   session_management.py — valid syntax
  OK:   structured_output.py — valid syntax
  OK:   subagents_orchestration.py — valid syntax

=== Python import resolution check ===
  File "<string>", line 1
    from claude_agent_sdk import (,AgentDefinition,ClaudeAgentOptions,...
                                  ^
SyntaxError: invalid syntax
  FAIL: Some template imports don't resolve
  FAIL:   Cannot import: (

=========================================
  Passed: 13
  Failed: 2
=========================================

TEMPLATE CHECK FAILED — 2 issue(s)
```

**Likely cause**: The import resolution check script generates a combined import statement that includes a stale leading comma (`,AgentDefinition,...`), producing invalid Python syntax. This is an instrumentation bug in the check script itself, not in the templates.

**Pipeline outcome**: Overridden to `success` in `outcomes` — this false-positive has recurred across multiple daily runs. No action needed on templates.

## Cost

| Agent | Cost | Turns |
|-------|------|-------|
| Update | $0.17 | 17 |
| Research (TS) | $2.00 | 82 |
| Research (PY) | $1.90 | 70 |
| Report | N/A | — |
| **Total** | **$4.07** | **169** |

## State

Current tracked state after today's run:

- **TypeScript SDK version**: 0.2.138
- **Python SDK version**: 0.1.80
- **TS last scanned issue**: #236
- **PY last scanned issue**: #776
- **TS tracked issues**: 59 (KIs #2–#41, many skipped/research-logged)
- **PY tracked issues**: 70 (KIs #1–#36, many skipped/research-logged)
- **TS known issues**: #2–#41 (with gaps)
- **PY known issues**: #1–#36 (with gaps)
- **TS last audited version**: 0.2.138
- **PY last audited version**: 0.1.56
- **State last updated**: 2026-05-11T09:01:27Z
